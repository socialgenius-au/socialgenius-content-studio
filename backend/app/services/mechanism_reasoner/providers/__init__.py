"""Mechanism reasoner provider adapters -- internal to app.services.mechanism_reasoner. External callers
should go through app.services.mechanism_reasoner.derive_mechanisms (the router-level entry point), never
import a concrete provider directly."""
