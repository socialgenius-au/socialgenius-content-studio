"""Hook reasoner provider adapters -- internal to app.services.hook_reasoner. External callers
should go through app.services.hook_reasoner.classify_hook (the router-level entry point), never
import a concrete provider directly."""
