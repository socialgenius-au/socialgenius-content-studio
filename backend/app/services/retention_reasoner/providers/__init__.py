"""Retention reasoner provider adapters -- internal to app.services.retention_reasoner. External
callers should go through app.services.retention_reasoner.classify_retention_candidate (the
router-level entry point), never import a concrete provider directly."""
